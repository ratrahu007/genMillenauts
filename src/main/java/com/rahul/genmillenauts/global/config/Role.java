package com.rahul .genmillenauts.global.config;

// Enum for all user roles in the platform
public enum Role {
    USER,         // Regular user
    THERAPIST,    // Therapist
    PEER_MEETUP   // Peer Meetup coordinator
}
